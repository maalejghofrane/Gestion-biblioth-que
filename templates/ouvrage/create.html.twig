{%  extends 'base.html.twig' %}
{% block body  %}
<div class="img_ouvrage">
    <h1> Formulaire :Ouvrage </h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Ajouter </button>
    {{ form_end(form) }}
    </div>
{% endblock %}