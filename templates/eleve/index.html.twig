{%  extends 'base.html.twig' %}
{% block body  %}
    <h1> Formulaire:Eleve </h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">Ajouter </button>
    {{ form_end(form) }}
{% endblock %}